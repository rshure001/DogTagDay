name: Publish Approved Film Crew Scene

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Archived workflow
        run: echo 'Homepage publishing is handled only by deploy-pages-now.yml.'
