name: Refresh Pages Domain
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: No-op domain refresh
        run: echo "Domain refresh disabled; Pages deploy workflow is authoritative."
