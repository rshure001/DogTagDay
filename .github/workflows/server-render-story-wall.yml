name: Archived - Server Render Story Wall
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Archived. Stories belong on stories.html and this workflow no longer modifies the homepage."
