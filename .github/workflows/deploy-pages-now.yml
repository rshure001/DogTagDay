name: Deploy Dog Tag Day Now

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages-production
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Verify current homepage
        run: |
          set -e
          test -s index.html
          grep -q '2026-09-15-counter-holidays-restored' index.html
          grep -q '4,300+' index.html
          grep -q 'holidayStrip' index.html
          grep -q 'camera-inline.svg' index.html
          test -s assets/hero-inline.svg
          test -s assets/combat-inline.svg
          test -s assets/bbq-inline.svg
          test -s assets/camera-inline.svg
      - name: Configure Pages
        uses: actions/configure-pages@v5
      - name: Upload site
        uses: actions/upload-pages-artifact@v3
        with:
          path: .
          name: github-pages-${{ github.run_id }}-${{ github.run_attempt }}
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
        with:
          artifact_name: github-pages-${{ github.run_id }}-${{ github.run_attempt }}
      - name: Verify production homepage
        run: |
          set -e
          for i in $(seq 1 36); do
            html=$(curl -fsSL -H 'Cache-Control: no-cache' "https://dogtagday.org/?verify=${GITHUB_SHA}-${GITHUB_RUN_ID}-$i" || true)
            if printf '%s' "$html" | grep -q '2026-09-15-counter-holidays-restored' && printf '%s' "$html" | grep -q '4,300+' && printf '%s' "$html" | grep -q 'holidayStrip' && printf '%s' "$html" | grep -q 'camera-inline.svg'; then
              echo LIVE_CURRENT_HOMEPAGE_VERIFIED
              exit 0
            fi
            sleep 5
          done
          exit 1
