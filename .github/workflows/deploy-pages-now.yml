name: Deploy Dog Tag Day Now

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages-production
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Validate current homepage
        run: |
          set -e
          test -s index.html
          grep -q '2026-09-16-final-homepage-v10' index.html
          grep -q 'assets/dog-tag-day-hero.PNG' index.html
          grep -q 'NO MAN LEFT BEHIND' index.html
          grep -q 'THE UNIFORM COMES OFF' index.html
          grep -q 'ronnie-street-conversation-final.jpg' index.html
          grep -q 'ronnie-founder-natural-facial-hair-20260903.jpg' index.html
          grep -q 'ronnie-community-support.jpg' index.html
          grep -q '4,817' index.html
          grep -q 'NEXT HOLIDAY:' index.html
          grep -q 'HALLOWEEN' index.html
      - name: Configure Pages
        uses: actions/configure-pages@v5
      - name: Upload site
        uses: actions/upload-pages-artifact@v3
        with:
          path: .
          name: github-pages-${{ github.run_id }}-${{ github.run_attempt }}
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
        with:
          artifact_name: github-pages-${{ github.run_id }}-${{ github.run_attempt }}
