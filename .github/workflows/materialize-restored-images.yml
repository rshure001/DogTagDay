name: Apply Dog Tag Day Visual Refinements

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  refine:
    runs-on: ubuntu-latest
    steps:
      - name: No automatic homepage overwrite
        run: echo "Legacy visual refinement is disabled from automatic pushes."
